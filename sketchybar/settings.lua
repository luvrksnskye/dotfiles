-- ╔═══════════════════════════════════════════════════════════════════╗
-- ║                   🌙 Skye's Settings 🌙                            ║
-- ╚═══════════════════════════════════════════════════════════════════╝

local M = {}

-- ═══════════════════════════════════════════════════════════════════════
--                          FONT CONFIGURATION
-- ═══════════════════════════════════════════════════════════════════════

M.font = {
    text = "SF Pro",
    numbers = "SF Mono",
    icons = "JetBrainsMono Nerd Font",
    
    -- Font styles
    style = {
        regular = "Regular",
        medium = "Medium",
        semibold = "Semibold",
        bold = "Bold",
        heavy = "Heavy"
    }
}

-- ═══════════════════════════════════════════════════════════════════════
--                              SIZING
-- ═══════════════════════════════════════════════════════════════════════

M.size = {
    -- Icon sizes
    icon = 16.0,
    icon_small = 14.0,
    icon_large = 18.0,
    
    -- Label sizes
    label = 13.0,
    label_small = 11.0,
    label_large = 14.0,
    
    -- Padding
    padding = 6,
    padding_small = 4,
    padding_large = 10,
    
    -- Bar
    bar_height = 38,
    bar_margin = 8,
    bar_y_offset = 8,
    bar_corner_radius = 12,
    
    -- Items
    item_height = 28,
    item_corner_radius = 10,
    
    -- Popup
    popup_corner_radius = 12,
    popup_border_width = 2
}

-- ═══════════════════════════════════════════════════════════════════════
--                           ANIMATION
-- ═══════════════════════════════════════════════════════════════════════

M.animation = {
    -- Curve types: "linear", "quadratic", "tanh", "sin", "exp", "circ"
    curve = "tanh",
    
    -- Duration (in animation frames, ~60fps)
    duration = {
        fast = 10,
        normal = 15,
        slow = 25,
        very_slow = 40
    }
}

-- ═══════════════════════════════════════════════════════════════════════
--                         BLUR & EFFECTS
-- ═══════════════════════════════════════════════════════════════════════

M.blur = {
    bar = 30,
    popup = 25
}

-- ═══════════════════════════════════════════════════════════════════════
--                           LOCATION
-- ═══════════════════════════════════════════════════════════════════════

-- Caracas, Venezuela 🇻🇪
M.location = {
    city = "Caracas",
    timezone = "America/Caracas",
    -- For weather API (wttr.in)
    query = "Caracas"
}

-- ═══════════════════════════════════════════════════════════════════════
--                        UPDATE FREQUENCIES
-- ═══════════════════════════════════════════════════════════════════════

M.update_freq = {
    clock = 30,           -- Every 30 seconds
    weather = 1800,       -- Every 30 minutes
    battery = 120,        -- Every 2 minutes
    wifi = 30,            -- Every 30 seconds
    media = 3,            -- Every 3 seconds (when not using events)
    cpu = 5,              -- Every 5 seconds
}

-- ═══════════════════════════════════════════════════════════════════════
--                           NOTCH
-- ═══════════════════════════════════════════════════════════════════════

-- Set to 0 if you don't have a MacBook with notch
M.notch_width = 200

-- ═══════════════════════════════════════════════════════════════════════
--                       PERSONAL GREETING
-- ═══════════════════════════════════════════════════════════════════════

M.greeting = {
    name = "Skye",
    morning = "Good morning",
    afternoon = "Good afternoon", 
    evening = "Good evening",
    night = "Sweet dreams"
}

return M
