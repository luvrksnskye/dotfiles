# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                                                                              ║
# ║   ████████╗███╗   ███╗██╗   ██╗██╗  ██╗                                      ║
# ║   ╚══██╔══╝████╗ ████║██║   ██║╚██╗██╔╝                                      ║
# ║      ██║   ██╔████╔██║██║   ██║ ╚███╔╝                                       ║
# ║      ██║   ██║╚██╔╝██║██║   ██║ ██╔██╗                                       ║
# ║      ██║   ██║ ╚═╝ ██║╚██████╔╝██╔╝ ██╗                                      ║
# ║      ╚═╝   ╚═╝     ╚═╝ ╚═════╝ ╚═╝  ╚═╝                                      ║
# ║                                                                              ║
# ║                                                                              ║
# ╚══════════════════════════════════════════════════════════════════════════════╝


set -g @ctp_pink      "#f5c2e7"
set -g @ctp_mauve     "#cba6f7"
set -g @ctp_flamingo  "#f2cdcd"
set -g @ctp_rosewater "#f5e0dc"
set -g @ctp_lavender  "#b4befe"

# Colores de soporte
set -g @ctp_text      "#cdd6f4"
set -g @ctp_subtext1  "#bac2de"
set -g @ctp_overlay0  "#6c7086"
set -g @ctp_surface2  "#585b70"
set -g @ctp_surface1  "#45475a"
set -g @ctp_surface0  "#313244"
set -g @ctp_base      "#1e1e2e"
set -g @ctp_mantle    "#181825"
set -g @ctp_crust     "#11111b"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          ⌨️  KEYBINDINGS                                     │
# └──────────────────────────────────────────────────────────────────────────────┘

# Cambiar prefix a Ctrl+a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Empezar ventanas y paneles desde 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set -g renumber-windows on

# Dividir paneles con | y -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind \\ split-window -h -c "#{pane_current_path}"
bind _ split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Abrir nuevas ventanas en el directorio actual
bind c new-window -c "#{pane_current_path}"

# Recargar configuración
bind r source-file ~/.tmux.conf \; display-message " 🌸 Config recargada!"

# Navegación entre ventanas
unbind p
unbind n
bind p previous-window
bind n next-window
bind -n C-Left previous-window
bind -n C-Right next-window

# Navegación entre paneles con Alt+flechas
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Navegación estilo vim
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Redimensionar paneles
bind -n S-Left resize-pane -L 2
bind -n S-Right resize-pane -R 2
bind -n S-Up resize-pane -U 1
bind -n S-Down resize-pane -D 1

# Cerrar panel/ventana
bind x confirm-before -p " 🌸 Cerrar panel? (y/n)" kill-pane
bind X confirm-before -p " 🌸 Cerrar ventana? (y/n)" kill-window

# Sincronizar paneles
bind S setw synchronize-panes

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          ⚙️  CONFIGURACIÓN GENERAL                           │
# └──────────────────────────────────────────────────────────────────────────────┘

set -sg escape-time 0
set -g allow-rename off
set -g automatic-rename off
set -g mouse on
set-window-option -g mode-keys vi
set -g history-limit 50000
set -g status-interval 1
set -g focus-events on

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          🖥️  CONFIGURACIÓN DE TERMINAL                       │
# └──────────────────────────────────────────────────────────────────────────────┘

set -g default-terminal "xterm-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -ag terminal-overrides ",*256col*:RGB"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          🔔 NOTIFICACIONES                                   │
# └──────────────────────────────────────────────────────────────────────────────┘

set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          📋 COPIAR Y PEGAR                                   │
# └──────────────────────────────────────────────────────────────────────────────┘

bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "pbcopy"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          🎨 ESTILO Y COLORES                                  │
# ── Colores de paneles (bordes rosados) ──
set -g pane-border-style "fg=#45475a"
set -g pane-active-border-style "fg=#f5c2e7"

# ── Modo de reloj ──
setw -g clock-mode-colour "#f5c2e7"

# ── Modo de selección ──
setw -g mode-style "fg=#1e1e2e bg=#f5c2e7 bold"

# ── Mensajes ──
set -g message-style "fg=#cdd6f4 bg=#313244 bold"
set -g message-command-style "fg=#cdd6f4 bg=#313244"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                     🌸 STATUS BAR (ARRIBA + BORDES REDONDEADOS)              │
# └──────────────────────────────────────────────────────────────────────────────┘

set -g status on
set -g status-position top
set -g status-justify left
set -g status-style "fg=#cdd6f4 bg=#1e1e2e"

# Longitud de las secciones
set -g status-left-length 100
set -g status-right-length 100

# ── IZQUIERDA: Icono terminal + Sesión ──
# Bordes redondeados:  (inicio)  (fin)
# Icono cambia de color cuando presionas prefix
set -g status-left "\
#[fg=#f5c2e7,bg=#1e1e2e]#{?client_prefix,#[fg=#b4befe],}\
#[fg=#1e1e2e,bg=#f5c2e7,bold]#{?client_prefix,#[bg=#b4befe],} 󰆍 #S \
#[fg=#f5c2e7,bg=#1e1e2e]#{?client_prefix,#[fg=#b4befe],} "

# ── DERECHA: Directorio + Calendario + Hora ──
set -g status-right "\
#[fg=#313244,bg=#1e1e2e]\
#[fg=#f2cdcd,bg=#313244]  #{b:pane_current_path} \
#[fg=#f5c2e7,bg=#313244]\
#[fg=#1e1e2e,bg=#f5c2e7]   %d %b \
#[fg=#cba6f7,bg=#f5c2e7]\
#[fg=#1e1e2e,bg=#cba6f7,bold]  󰥔 %H:%M \
#[fg=#cba6f7,bg=#1e1e2e]"

# ── VENTANAS ──
setw -g window-status-separator ""

# Ventana activa (rosa con bordes redondeados)
setw -g window-status-current-format "\
#[fg=#f5c2e7,bg=#1e1e2e]\
#[fg=#1e1e2e,bg=#f5c2e7,bold]  #I  #W #{?window_zoomed_flag,󰊓 ,}\
#[fg=#f5c2e7,bg=#1e1e2e]"

# Ventanas inactivas (lavanda suave con bordes redondeados)
setw -g window-status-format "\
#[fg=#45475a,bg=#1e1e2e]\
#[fg=#bac2de,bg=#45475a]  #I  #W #{?window_bell_flag,󰂞 ,}\
#[fg=#45475a,bg=#1e1e2e]"

# Estilo de ventanas con actividad
setw -g window-status-activity-style "fg=#f5e0dc,bg=#45475a"
setw -g window-status-bell-style "fg=#f5c2e7,bg=#45475a,bold"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          🚀 ATAJOS EXTRA                                     │
# └──────────────────────────────────────────────────────────────────────────────┘

bind N new-session
bind s choose-tree -Zs
bind m resize-pane -Z
bind C-l send-keys 'C-l' \; clear-history

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │                          📝 ATAJOS RÁPIDOS                                   │
# └──────────────────────────────────────────────────────────────────────────────┘

# Ctrl+a |     → Dividir verticalmente
# Ctrl+a -     → Dividir horizontalmente
# Ctrl+a r     → Recargar config
# Ctrl+a c     → Nueva ventana
# Ctrl+a p/n   → Ventana anterior/siguiente
# Ctrl+a h/j/k/l → Navegar paneles (vim)
# Alt+flechas  → Navegar paneles
# Shift+flechas → Redimensionar paneles
# Ctrl+a m     → Maximizar/restaurar panel
# Ctrl+a s     → Lista de sesiones

# 🌸 Requiere: Nerd Font instalada
